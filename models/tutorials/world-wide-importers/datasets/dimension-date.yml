unique_name: dimension-date
object_type: dataset
label: date
connection_id: Connection - WW
table: date

columns:
  - name: calendar_month_label
    data_type: string
  - name: CALENDAR_MONTH_NUMBER
    data_type: long
  - name: CALENDAR_YEAR
    data_type: long
  - name: calendar_year_label
    data_type: string
  - name: date
    data_type: date
  - name: DAY
    data_type: long
  - name: DAY_NUMBER
    data_type: long
  - name: fiscal_month_label
    data_type: string
  - name: FISCAL_MONTH_NUMBER
    data_type: long
  - name: FISCAL_YEAR
    data_type: long
  - name: fiscal_year_label
    data_type: string
  - name: ISO_WEEK_NUMBER
    data_type: long
  - name: MONTH
    data_type: string
  - name: SHORT_MONTH
    data_type: string
  - name: year_month
    data_type: string
    sql: "EXTRACT(YEAR from date) || LPAD(EXTRACT(MONTH from date),2,'0')"
    dialects:
      - dialect: Snowflake 
        sql: "EXTRACT(YEAR from date) || LPAD(EXTRACT(MONTH from date),2,'0')"
